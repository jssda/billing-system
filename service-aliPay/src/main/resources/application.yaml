spring:
  application:
    name: alipay-server
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://123.57.39.124:3306/billing_system
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: liuHao123.
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 8900
eureka:
  instance:
    #    hostname: localhost
    hostname: 123.57.39.124
  client:
    # 是否当作客户端注册到服务端, 默认为true
    register-with-eureka: true
    # 是否从Eureka获取注册信息
    fetch-registry: false
    serviceUrl:
      # 设置与Eureka Server交互的地址, 多个地址可使用','分隔。
      defaultZone: http://123.57.39.124:8761/eureka/



logging.level.com.debug.steadyjack: debug
logging.level.org.mybatis.spring: debug

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  file:
    path: ./logs
    name: alipayService.log
